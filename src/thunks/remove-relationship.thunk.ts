type RemoveRelationshipInput = {
    from: string;
    to: string;
    relationship: string;
}

export function removeRelationship(input){
    // TODO: handle remove relationship frontend things (eg. give from and delete everything from it)
    // Backend requires both from and to
}